# Organism Handover — 2026-04-07

## Session Summary

Full Synapse review completed with the new Organism architecture. Two vault outputs produced:
- `vault/synapse/strategic-review-2026-04-07.md` — PMF, 80/20 analysis, 30/90/180 roadmap
- `vault/synapse/financial-analysis-2026-04-07.md` — unit economics, burn rate, breakeven model

### Cost Comparison (Old vs New Organism)

| Metric | Old (Apr 6) | New (Apr 7) | Change |
|--------|-------------|-------------|--------|
| Total cost | $88.06 | $54.37 | **-38%** |
| Quality Guardian | $37.79 (Opus, 1 task) | $3.20 (Sonnet, 20 tasks) | **-92%** |
| Primary agents avg | $2-6 each | $0.14 each | **-95%** |
| Security audit | $11.22 | $19.25 | **+71% (bug)** |
| Task count | ~60 | ~107 | +78% (more thorough) |

**Target for next run: under $20** by fixing security-audit overspend and tightening revision loops.

---

## Bugs to Fix (Priority Order)

### 1. Security-audit overspend — $19.25 (was $11.22)
**What:** Trigger-based reviews created too many security tasks for HIGH-lane items. One task hit $15.48 (20,536% over estimate).
**Where:** `agents/security-audit/CLAUDE.md` + `packages/core/src/context-brief.ts` (security-audit context scoping)
**Fix:** Cap security-audit output payload size (like quality-guardian already has). Tighten trigger conditions so security-audit doesn't fire on every HIGH task — only tasks touching auth, API, data exposure, or infra. Budget cap exists at $25/day (`budget.ts:39`) but per-task estimate is only $0.50 for HIGH lane (`budget.ts:54`) — the mismatch means overspend alerts fire but don't prevent the spend.

### 2. StixDB 422 errors
**What:** Cross-agent memory (StixDB) writes are failing with 422. Agents can't share findings via memory.
**Fix:** Check the StixDB schema at `state/stixdb/` — likely a schema mismatch or missing field. If unfixable quickly, disable StixDB writes cleanly (don't let 422s pollute logs).

### 3. Codex review returning $0
**What:** `OPENAI_API_KEY` not set. All 20 codex reviews returned $0/no output.
**Fix:** Either set `OPENAI_API_KEY` in environment, or disable codex-review agent from the pipeline until the key is configured. Currently wasting task slots.

### 4. Product-manager revision overspend — $8.14 on one task
**What:** Quality feedback loop creates revision tasks that are expensive. PM revision cycles are unbounded.
**Fix:** Cap revision iterations (max 2 revisions per task) or cap PM task budget at $2.00.

---

## What Was NOT Completed This Session

1. **Perspectives run** — was kicked off (`npm run organism "perspectives synapse"`) but session hit rate limit before results came back. Check if output landed in `vault/synapse/`.
2. **Security-audit bug fix** — previous session started reading the CLAUDE.md but didn't edit anything. Fix is still needed.
3. **Handover was interrupted** — the comparison tables (pasted by Rafael) were generated but the session ended before writing this document.

---

## System State

| Component | Status |
|-----------|--------|
| Daemon | Started 2026-04-06T10:15:30Z, next review cycle overdue (was scheduled 17:00) |
| Dashboard | Deployed on Vercel + Turso — **don't touch without asking Rafael** |
| G4 gate | Active — HIGH-risk tasks require Rafael's approval |
| Rate limit | Hit limit at ~3:03am AEST, resets 4am |
| Vault outputs | 2 new files in `vault/synapse/` from this run |

---

## Key Findings from This Review

### Strategic (vault/synapse/strategic-review-2026-04-07.md)
- ANZCA is the right beachhead — no competitor offers MCQ+SAQ+VIVA in one platform
- **Two blockers before launch:** SAQ citations ($85) + VIVA model_answer ($380) = $465 total
- Auth bypass status disputed — financial analysis says `BYPASS_AUTH = false` (correct per code), strategic review says `true` (stale). Trust the code.
- 30/90/180 roadmap: auth → enrichment → beta (30d) → Stripe + CICM (90d) → ACEM + public (180d)

### Financial (vault/synapse/financial-analysis-2026-04-07.md)
- 90-94% gross margin at AUD $49/mo
- Operational breakeven: **4 subscribers**
- Phase 1 enrichment ($465) payback at 10 subs: **2.1 months**
- All enrichment ($1,949 total) payback at 30 subs: **2.3 months**
- Monthly burn: ~AUD $138/mo (midpoint)

---

## Next Session Priorities

1. **Fix security-audit overspend** — cap output payload, tighten triggers
2. **Fix StixDB 422s** — or disable cleanly
3. **Set OPENAI_API_KEY** or disable codex-review
4. **Cap PM revision loops** — max 2 iterations or $2 budget
5. **Re-run review** — target: under $20 total cost
6. **Check perspectives output** — may have landed in vault before rate limit hit

---

## Open Questions for Rafael (from review outputs)

1. Pricing: AUD $49/mo or $59/mo for ANZCA? Business plan says $59, financial model used $49.
2. CICM timing: concurrent with ANZCA or sequential (ANZCA revenue funds CICM)?
3. ACEM: Year 1 or Year 2 target? If Year 2, defer $160-250 enrichment entirely.
4. User interviews: plan to talk to 10 ANZCA Primary candidates before paywall?
